generated: '2026-09-04'
method: searched
source: https://docs.shieldlabs.ai/.well-known/agent-skills/shieldlabs/skill.md
note: >-
  ShieldLabs publishes its own Agent Skill and advertises it from the skills[0].url of its A2A agent
  card. Saved verbatim — not generated by API Evangelist.

  Re-fetched 2026-09-04 (HTTP 200) and the provider had REWRITTEN it. The new version is materially
  better as an agent artifact: it opens with the situations that should trigger the skill rather than
  a feature list, and it adds an explicit negative boundary — "Do not use ShieldLabs for:
  authentication (it identifies visitors, not users), authorization (it scores risk, not
  permissions), or as a standalone blocker (it surfaces evidence; your code decides)". A skill that
  tells an agent when NOT to reach for it is rare. Saved verbatim over the previous copy.
skills:
- file: shieldlabs-shieldlabs.md
  name: Shieldlabs
  description: >-
    Use when building visitor identification, fraud detection, and abuse prevention systems. Reach for
    this skill when implementing risk scoring, anonymity detection (VPN/proxy/Tor), account security
    (login, signup, payment), or traffic quality analysis. Apply when you need to detect
    multi-accounting, account takeover, credential stuffing, payment fraud, promo abuse, or bot
    traffic.
  api: openapi/shieldlabs-server-api-openapi.yml
  operations:
  - searchHistoryAccount
  - getProfileV1
  - searchHistoryV1
  method: searched
  source: https://docs.shieldlabs.ai/.well-known/agent-skills/shieldlabs/skill.md
  provider_published: true
